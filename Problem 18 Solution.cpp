/* C program to check whether the given term 𝑛 is a term of the Fibonacci Series and returns the string “YES” or “NO” without quotes accordingly */

#include <stdio.h>
#include <math.h>
int main ()
{
int x;
{
	int s = (int) sqrt (x);
	return (s*s==x);
}

{
	return is perfect sqaure (5*x*x+4) || is perfect square (5*x*x-4);
}
int main () {
	int n, s;
	if (n>=0 && n<=1000)
}
{
printf ("Enter a number :");
scanf ("%d", &n);
if (is Fibonacci (n))
printf ("YES");
else
printf ("NO");
}
else
printf ("Out of range");
return 0;
}